[project]
name = "medex-ml-models"
version = "0.1.0"
dependencies = [
    "flask",
    "flask-cors",
    "joblib",
    "numpy",
    "pandas",
    "scikit-learn==1.8.0",
    "python-dotenv",
]

[tool.vercel]
entrypoint = "medex_ml_api/app.py"
